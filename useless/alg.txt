if(getLEFT.isexist)
	if(getTOP.isexist)
		setTableIDsameofTOP
	else
		if(reachTOP.tableid == LEFT.tableid) 
			setTableIDsameofLEFT
		else
			createNewTable
			setTableID
else
	if(getTOP.isexist)
		setTableIDsameofTOP
	else
		if(reachTOP.columnid == this.columnid && reachLEFT.rowid = this.rowid && LEFT,TOP are sameTABLE)
			setTableIDsameofREACHEDCELL
		elseif(reachTOP.tableid == toprowcontains.tableid)
			setTableIDsameofREACHEDCELL
		else
			createNewTable
			setTableID


TBoundary uses real positions
TWorkbook uses relative positions
Mapper uses real positions

reachTOP
reachLEFT

if(reachLEFT.exist)
	if(reachTOP.exist)
		if(reachLEFT.tableid == reachTOP.tableid)
			expandTableOf reachTOP/reachLEFT
		else
			if(toprowcontains.reachLEFT)
				createNewTable
				setTableID
			else
				expandTableOf reachLEFT
	else
		
else
	if(reachTOP.exist)

	else